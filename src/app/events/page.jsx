import EventsPageContent from "./EventsPageContent";

export const metadata = {
  title: "Events | SuccessWikis",
  description: "Join SuccessWikis exclusive gatherings, offline founder meets, and networking mixers designed to spark meaningful collaborations and insights.",
};

export default function EventsPage() {
  return <EventsPageContent />;
}
